#ifndef HISTORIC_CENTER_POINT_H
#define HISTORIC_CENTER_POINT_H

class Point {
public:
    float x, z, y;

    Point();

    Point(double x, double y, double z);

    Point(float x, float y);
};

#endif //HISTORIC_CENTER_POINT_H
